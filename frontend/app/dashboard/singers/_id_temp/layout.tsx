// Generate static params for build
export async function generateStaticParams() {
  return [];
}

export default function SingerLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
